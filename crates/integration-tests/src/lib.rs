//! Workspace-level integration tests for Emberforge.
//!
//! This crate intentionally contains no production code. It is a home for
//! cross-crate integration tests (under `tests/`) that wire several crates
//! together through their public APIs — coverage that per-crate unit tests
//! cannot provide. See `tests/` for the actual test suites.
